import React from "react";
import TestFunction from "../(pages)/Testing/TestFunction"
import DiamondInteractive from "../(components)/DiamondInteractive";

export default function Test() {
  return (
    <div>
      <TestFunction />
      <DiamondInteractive />
      <p>This is a test page to verify the setup in the new directory structure.</p>
    </div>
  );
}